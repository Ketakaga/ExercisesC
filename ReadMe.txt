The mdf file is a MS Server db containing 2 tables:

- a list of bank account users with bank account num and passwords
- A list of fake bank accounts

The app allows fake bank accounts to create one users (if none exists at the moment)
There are specific rules on how to set a password
Only existing bank accounts can create a user